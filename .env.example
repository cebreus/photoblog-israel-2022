# Gallery configuration
CONTENT_DIR=egypt-2025
OUTPUT_DIR=build

# Map tiles configuration
# Set to 'true' to use locally downloaded tiles (requires running download-tiles script)
# Set to 'false' or leave unset to use CDN tiles (smaller bundle, requires internet)
PUBLIC_USE_LOCAL_TILES=true
